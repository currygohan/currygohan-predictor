/**
 * Phase 2: weighted analysis + mixer returns two suggested sets per game.
 * @param {"mega_millions"|"powerball"} game
 * @param {object[]} historyRows normalized draw rows from the UI layer
 * @returns {null | { setA: { whites: number[], bonus: number }, setB: { whites: number[], bonus: number } }}
 */
export function suggestSets(game, historyRows) {
  void game;
  void historyRows;
  return null;
}
