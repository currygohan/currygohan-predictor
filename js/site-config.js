/**
 * Site-wide settings (safe to commit — GA measurement IDs are public in the browser).
 *
 * Google Analytics 4: create a property at https://analytics.google.com/
 * → Admin → Data streams → Web → copy Measurement ID (G-XXXXXXXXXX).
 * Leave empty to disable analytics.
 */
export const GA_MEASUREMENT_ID = "G-1PK9THYQPS";
